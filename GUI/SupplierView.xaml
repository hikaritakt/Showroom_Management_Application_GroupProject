<Page x:Class="ShowroomApp.GUI.SupplierView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="SupplierView" FontFamily="Segoe UI" Background="Transparent">
    
    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🏢 Manage Suppliers" FontSize="24" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>
        
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="1" Opacity="0.04"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="COMPANY NAME" Foreground="#64748B" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtName" Width="180" Height="36" Padding="8,0" VerticalContentAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="ADDRESS" Foreground="#64748B" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtAddress" Width="180" Height="36" Padding="8,0" VerticalContentAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Margin="0,0,25,0">
                    <TextBlock Text="PHONE NUMBER" Foreground="#64748B" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtPhone" Width="180" Height="36" Padding="8,0" VerticalContentAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="➕ Add" Width="85" Height="36" Background="#10B981" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="BtnAdd_Click"/>
                    <Button Content="✏️ Edit" Width="85" Height="36" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="BtnUpdate_Click"/>
                    <Button x:Name="btnDelete" Content="🗑️ Delete" Width="85" Height="36" Background="#EF4444" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnDelete_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="1" Opacity="0.04"/>
            </Border.Effect>
            <DataGrid x:Name="dgSuppliers" AutoGenerateColumns="False" IsReadOnly="True" 
                      SelectionChanged="DgSuppliers_SelectionChanged" 
                      Background="White" BorderThickness="0" RowHeight="40" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E2E8F0"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding SupplierId}" Width="80"/>
                    <DataGridTextColumn Header="Supplier Name" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="Address Location" Binding="{Binding Address}" Width="2*"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="1.5*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
