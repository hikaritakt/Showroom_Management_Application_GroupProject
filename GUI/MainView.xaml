<Window x:Class="ShowroomApp.GUI.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Showroom Management - Dashboard" Height="700" Width="1100" WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" Background="#F0F2F5">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="SidebarBtn">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CBD5E1"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="25,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#334155"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#1E293B">
            <StackPanel Margin="0,30,0,0">
                <TextBlock Text="SHOWROOM" FontSize="26" FontWeight="Black" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,2"/>
                <TextBlock Text="MANAGEMENT SYSTEM" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,0,0,40"/>
                
                <!-- User Profile Card -->
                <Border Background="#334155" Margin="20,0,20,30" Padding="15" CornerRadius="10">
                    <StackPanel>
                        <TextBlock Text="LOGGED IN AS:" FontSize="10" Foreground="#94A3B8" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock x:Name="txtWelcome" Text="User" FontSize="15" Foreground="White" FontWeight="Bold" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Navigation Buttons -->
                <Button x:Name="btnEmployee" Content="👥  Manage Employees" Style="{StaticResource SidebarBtn}" Click="BtnEmployee_Click"/>
                <Button x:Name="btnSupplier" Content="🏢  Manage Suppliers" Style="{StaticResource SidebarBtn}" Click="BtnSupplier_Click"/>
                
                <!-- Flexible space or fixed margin for Logout -->
                <Button x:Name="btnLogout" Content="🚪  Logout" Style="{StaticResource SidebarBtn}" Click="BtnLogout_Click" Margin="0,200,0,0" Foreground="#F87171"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="White" Margin="20" CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            <!-- Content Frame -->
            <Frame x:Name="mainFrame" NavigationUIVisibility="Hidden"/>
        </Border>
    </Grid>
</Window>
